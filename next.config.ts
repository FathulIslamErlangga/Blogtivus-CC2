import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["dicoding-assets.sgp1.cdn.digitaloceanspaces.com"], // Tambahkan hostname di sini
  },
};

export default nextConfig;
